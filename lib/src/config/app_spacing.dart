class AppSpacing {
  AppSpacing._();
  static const double ZERO = 0;
  static const double XS = S / 2;
  static const double S = M / 1.5;
  static const double M = 8.0;
  static const double L = M * 1.5;
  static const double XL = L * 2;
  static const double XXL = XL * 2;
}
